import "../scss/button.scss";

// The hover swap animation is pure CSS (:hover + transform transitions), so
// no JS is required to drive it. This file exists to keep the same
// enqueue/build pattern used by every other widget in the plugin.
